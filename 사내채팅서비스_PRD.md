# 사내/협력사 소통방 서비스 — PRD (Product Requirements Document)

버전: v1.0 (테스트/MVP 기준)
작성일: 2026-09-17
용도: Claude Code에 그대로 전달하여 단계별(Phase) 구현 진행

---

## 1. 개요

카카오톡/슬랙과 유사한 형태의 **사내 및 외부 협력사 공용 대화방 서비스**.
비용 없이(무료 티어), 인터넷 연결만으로 접속 가능하며, 저장 용량을 최소화한 경량 구조로 설계한다.

### 1.1 핵심 컨셉
- 사용자는 **이름만 간단히 입력**하고 방(room)에 입장
- 방은 **초대 링크**로 공유 (회원가입/로그인 절차 없음 — 마찰 최소화)
- 대화 내용은 **최대 30일 보관 후 자동 삭제**
- 이미지, 엑셀(xlsx), 파워포인트(pptx) 등 **파일 첨부 가능**
- 서버/DB는 **무료 티어**로 운영 가능한 구조

---

## 2. 목표 (Goals)

1. 별도 앱 설치 없이 **웹 브라우저**로 접속 가능
2. **방 생성 → 링크 공유 → 이름 입력 후 입장** 흐름을 3단계 이내로 단순화
3. 텍스트 대화 + 파일 첨부(이미지/xlsx/pptx/pdf 등) 지원
4. 저장 데이터는 **30일 경과 시 자동 삭제**되어 용량 관리
5. 초기 운영비 **0원**으로 시작 가능한 구조

## 2.1 비목표 (Non-Goals, 이번 단계에서 제외)
- 정식 로그인/회원가입 시스템 (이메일 인증, 비밀번호 등)
- 읽음 확인, 알림(푸시/이메일)
- 음성/영상 통화
- 메시지 검색, 스레드/답글 기능
- 세밀한 권한 관리(관리자/멤버 구분) — 최소 기능만

> 위 항목은 MVP 이후 확장 단계(백로그)에서 필요 시 추가

---

## 3. 사용자 시나리오

1. **방 생성자(사내 담당자)**: 사이트 접속 → "방 만들기" 클릭 → 방 이름 입력 → 초대 링크 생성 → 협력사에 링크 전달(카톡/이메일 등)
2. **참여자(협력사 직원 등)**: 받은 링크 클릭 → 표시 이름 입력(로그인 없음) → 방 입장 → 대화 및 파일 공유
3. **자동 정리**: 매일 백그라운드 작업이 실행되어 생성된 지 30일 지난 메시지/첨부파일을 자동 삭제

---

## 4. 기능 요구사항

### 4.1 방(Room) 관리
- 방 생성 시 고유 코드/슬러그 자동 생성 (예: `/room/ab3f9x`)
- 방 이름, 생성일, 만료 예정일(생성일+30일 자동 표시) 저장
- 별도 승인 절차 없이 **링크를 아는 사람은 누구나 입장** 가능 (초기 MVP 기준 — 보안 최소화 요청 반영)
- 방 목록/이력 관리 화면은 생략 (필요 시 2단계에서 추가)

### 4.2 입장/이름 설정
- 로그인 없이 **닉네임(표시 이름)만 입력** 후 입장
- 닉네임은 브라우저에 임시 저장(localStorage)하여 재접속 시 자동 입력
- 동일 닉네임 중복 허용 (구분자 자동 부여, 예: "홍길동", "홍길동(2)")

### 4.3 채팅
- 실시간 텍스트 메시지 송수신
- 보낸 사람 이름 + 시간 표시
- 최근 메시지부터 스크롤하여 과거 메시지 확인 (최대 30일치까지만 존재)
- 메시지 삭제/수정 기능은 MVP 제외 (필요 시 확장)

### 4.4 파일 첨부
- 지원 형식: 이미지(jpg/png/gif/webp), PDF, 엑셀(xlsx), 파워포인트(pptx), 워드(docx), 텍스트/CSV
- 파일당 용량 제한: **10MB 권장** (무료 스토리지 한도 고려)
- 업로드된 파일은 채팅창에 썸네일(이미지) 또는 파일 아이콘 + 다운로드 링크로 표시
- 첨부파일도 메시지와 함께 30일 후 자동 삭제

### 4.5 자동 삭제 (핵심 요구사항)
- 매일 1회(새벽 시간대) 배치 작업(cron) 실행
- 생성일 기준 **30일 초과 메시지 + 첨부파일**을 DB 및 스토리지에서 완전 삭제
- 30일 이상 활동 없는 방은 방 자체도 함께 정리(선택 사항, 2단계에서 결정)

---

## 5. 비기능 요구사항

| 항목 | 요구 수준 |
|---|---|
| 비용 | 무료 티어 내에서 운영 (신용카드 등록 불필요한 곳 우선) |
| 접속 방식 | 인터넷 연결만으로 접속 (통신사 별도 연동 불필요) |
| 저장 용량 | 최소화 — 텍스트는 DB에 압축 저장, 파일은 별도 스토리지에 최소 보관 기간(30일)만 유지 |
| 보안 | 이번 단계는 **최소화** (로그인/암호화 없이 링크 기반 접근). 단, 실제 업무 데이터 사용 전 최소한의 접근 제어(방 비밀번호 등)는 2단계에서 검토 권장 |
| 동시 접속 | 소규모(협력사 포함 수십 명 이내) 기준 |
| 반응 속도 | 실시간에 가까운 전송(수 초 이내) |

---

## 6. 기술 스택 (무료 운영 기준)

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | React (Vite) | 가볍고 빠른 빌드, Claude Code 친화적 |
| 실시간 통신 | Socket.io | 무료, 검증된 실시간 채팅 표준 |
| 백엔드 | Node.js (Express) | Socket.io와 궁합 좋음, 배포 쉬움 |
| DB | Supabase (PostgreSQL, 무료 티어) | 무료로 DB + 파일 스토리지(Storage) 동시 제공 |
| 파일 스토리지 | Supabase Storage (무료 1GB) | 별도 스토리지 서버 불필요 |
| 배포(백엔드) | Render 또는 Railway 무료 티어 | 카드 없이 시작 가능, GitHub 연동 자동 배포 |
| 배포(프론트) | Vercel 또는 Render Static | 무료, GitHub 연동 |
| 자동 삭제 배치 | node-cron (서버 내) 또는 Supabase의 pg_cron | 별도 서버 없이 스케줄 작업 가능 |

> **용량 최소화 팁**: 메시지 텍스트는 압축 없이 저장해도 텍스트 특성상 용량이 매우 작음. 실제 용량을 차지하는 건 파일 첨부이므로, 30일 자동 삭제 정책이 용량 관리의 핵심 장치가 됨.

---

## 7. 데이터 모델 (최소 스키마)

```
rooms
  id (PK)
  slug (고유 링크 코드)
  name
  created_at
  expires_at (created_at + 30일, 참고용)

messages
  id (PK)
  room_id (FK)
  sender_name (닉네임 텍스트, 로그인 없음)
  content (텍스트, nullable)
  attachment_url (nullable)
  attachment_type (image/pdf/xlsx/pptx/docx 등, nullable)
  attachment_size (nullable)
  created_at
```

- 별도 `users` 테이블 없음 (로그인 없는 구조이므로 최소화)
- 인덱스: `messages.room_id`, `messages.created_at` (자동 삭제 배치 성능용)

---

## 8. 단계별 개발 계획 (Claude Code 진행용)

### Phase 0. 프로젝트 초기 설정
- GitHub 저장소 생성
- 프론트(Vite+React), 백엔드(Node+Express) 기본 골격 구성
- Supabase 프로젝트 생성 (무료), DB 스키마 위 구조로 생성

### Phase 1. 방 생성 & 입장 흐름
- 방 생성 API (`POST /rooms`) → 슬러그 발급
- 방 입장 페이지: 닉네임 입력 폼 → localStorage 저장
- 방 링크(`/room/:slug`) 접속 시 입장 처리

### Phase 2. 실시간 채팅 기능
- Socket.io 서버 설정 (방별 room 채널 분리)
- 텍스트 메시지 송수신 + 화면 렌더링
- 메시지 DB 저장 (`messages` 테이블)

### Phase 3. 파일 첨부 기능
- 프론트: 파일 선택 UI, 업로드 진행 표시
- 백엔드: Supabase Storage 업로드 API
- 채팅창에 이미지 미리보기 / 기타 파일은 아이콘+다운로드 링크로 표시

### Phase 4. 자동 삭제 배치
- node-cron으로 매일 1회 실행되는 정리 작업 작성
- 30일 초과 `messages` row 삭제 + 연결된 Storage 파일 삭제
- (선택) 30일 이상 비활동 방 자체 삭제 로직

### Phase 5. 배포 & 테스트
- 백엔드 → Render/Railway 무료 배포
- 프론트 → Vercel 무료 배포
- 실제 협력사 테스트 계정으로 링크 공유 후 동작 확인

### Phase 6. (선택) 향후 확장 백로그
- 방 입장 시 간단한 비밀번호/PIN 설정 (보안 최소 강화)
- 메시지 검색
- 읽음 표시, 알림
- 관리자용 방 목록 대시보드

---

## 9. 리스크 및 참고 사항

- **무료 티어 한계**: Render 무료 서버는 일정 시간 미사용 시 슬립 상태가 되어 재접속 시 수십 초 지연 발생 가능. 테스트 단계에서는 문제없으나, 실사용 전환 시 고려 필요.
- **보안 최소화의 트레이드오프**: 로그인 없이 링크만으로 입장 가능하므로, 링크가 외부로 유출되면 누구나 대화 내용을 볼 수 있음. 실제 업무 민감 정보를 다룰 경우 최소한의 방 비밀번호 기능 추가를 권장.
- **파일 저장 한도**: Supabase 무료 스토리지는 약 1GB. 30일 자동 삭제 정책과 함께라면 소규모 팀 기준으로는 충분.

---

## 10. Claude Code 시작 프롬프트 예시

Claude Code를 열고 아래처럼 요청하면 Phase 0부터 바로 시작할 수 있습니다.
(※ 사내 로고/이미지 등 디자인 파일을 프로젝트 폴더 안에 미리 넣어두셨다면, 아래처럼 폴더 경로를 함께 언급해주세요. Claude Code가 해당 폴더를 직접 확인해서 색상·로고·톤을 반영합니다.)

> "위 PRD 기준으로 Phase 0(프로젝트 초기 설정)부터 시작해줘. React(Vite) 프론트엔드, Node.js+Express+Socket.io 백엔드, Supabase DB/Storage 연동 구조로 만들고, 방 생성/입장/실시간 채팅/파일첨부/30일 자동삭제까지 단계별로 진행해줘.
> 그리고 디자인은 내가 [디자인 파일 폴더 경로, 예: ./design-assets] 폴더에 사내 로고/이미지 파일을 넣어뒀으니, 이 파일들을 확인하고 색상·폰트·전체적인 톤을 반영해서 사내 브랜드에 맞는 UI로 만들어줘."

**참고**: 폴더 경로는 실제 파일을 넣어둔 위치로 바꿔서 입력하시면 됩니다(예: 프로젝트 루트의 `/design-assets`, `/assets/brand` 등). Claude Code는 해당 경로의 이미지 파일을 직접 열어보고 로고 형태, 주요 색상, 분위기를 분석해서 반영할 수 있습니다.

